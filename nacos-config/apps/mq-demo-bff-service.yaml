feign:
  client:
    config:
      message-service:
        connectTimeout: 3000
        readTimeout: 10000

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus,metrics
  metrics:
    tags:
      application: ${spring.application.name}
