server:
  port: 8086

spring:
  application:
    name: sentinel-bff-service
  config:
    import: optional:nacos:${spring.application.name}.yaml
  cloud:
    nacos:
      discovery:
        server-addr: ${NACOS_ADDR:127.0.0.1:8848}
      config:
        server-addr: ${NACOS_ADDR:127.0.0.1:8848}
        file-extension: yaml
    sentinel:
      eager: true
      transport:
        dashboard: ${SENTINEL_DASHBOARD:127.0.0.1:8858}
      datasource:
        bff-flow:
          nacos:
            server-addr: ${NACOS_ADDR:127.0.0.1:8848}
            data-id: sentinel-bff-service-flow-rules.json
            group-id: SENTINEL_GROUP
            data-type: json
            rule-type: flow
        bff-degrade:
          nacos:
            server-addr: ${NACOS_ADDR:127.0.0.1:8848}
            data-id: sentinel-bff-service-degrade-rules.json
            group-id: SENTINEL_GROUP
            data-type: json
            rule-type: degrade

feign:
  sentinel:
    enabled: true

management:
  endpoints:
    web:
      exposure:
        include: health,info

sentinel:
  cluster:
    mode: ${SENTINEL_CLUSTER_MODE:off}
    server:
      host: ${SENTINEL_CLUSTER_SERVER_HOST:127.0.0.1}
      port: ${SENTINEL_CLUSTER_SERVER_PORT:18730}
    client:
      request-timeout: ${SENTINEL_CLUSTER_CLIENT_REQUEST_TIMEOUT:20}
